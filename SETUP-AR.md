# نظام الحضور والانصراف - دليل التثبيت والتشغيل

## المتطلبات الأساسية

| البرنامج     | الإصدار الأدنى | الرابط |
|-------------|---------------|-------|
| Node.js     | 18+           | nodejs.org |
| MySQL       | 8.0+          | mysql.com |
| npm         | 9+            | مضمن مع Node.js |

---

## أولاً: إعداد قاعدة البيانات

افتح MySQL Command Line أو Workbench وشغّل:

```sql
CREATE DATABASE zkteco_attendance
  CHARACTER SET utf8mb4
  COLLATE utf8mb4_unicode_ci;
```

---

## ثانياً: إعداد الخادم الخلفي (Backend)

```bash
cd backend

# نسخ ملف الإعدادات
copy .env.example .env
```

افتح الملف `backend/.env` وعدّل:

```env
DATABASE_URL="mysql://root:كلمة_المرور@localhost:3306/zkteco_attendance"
JWT_SECRET="مفتاح-سري-عشوائي-لا-تشاركه"
PORT=5000
NODE_ENV=development
```

ثم ثبّت المكتبات وهيّئ قاعدة البيانات:

```bash
npm install
npx prisma migrate dev --name init
node prisma/seed.js
```

عند اكتمال `seed.js` ستظهر:
```
Seed complete.
Admin login: admin / admin123
```

---

## ثالثاً: إعداد الواجهة (Frontend)

```bash
cd frontend
npm install
```

---

## رابعاً: تشغيل وضع التطوير

### الطريقة الأسهل (ملف واحد):

```batch
start-dev.bat
```

يفتح تلقائياً:
- ✅ الخادم الخلفي (المنفذ 5000)
- ✅ واجهة المستخدم Vite (المنفذ 3002)
- ✅ نافذة Electron

### أو يدوياً في 3 نوافذ:

**نافذة 1 — الخادم:**
```bash
cd backend
node src/index.js
```

**نافذة 2 — الواجهة:**
```bash
cd frontend
npm run dev
```

**نافذة 3 — البرنامج:**
```bash
cd frontend
npx electron .
```

---

## خامساً: بناء الإصدار النهائي (.exe)

```batch
build-app.bat
```

أو يدوياً:

```bash
cd frontend
npm run build        # بناء الواجهة
npx electron-builder --win --x64
```

النتيجة في: `dist-electron/`
```
dist-electron/
  AttendanceSystemSetup.exe   ← المثبّت النهائي
```

---

## سادساً: إضافة جهاز البصمة

1. افتح **الأجهزة** من القائمة الجانبية
2. انقر **إضافة جهاز**
3. أدخل:
   - اسم الجهاز: `بوابة المدخل الرئيسي`
   - عنوان IP: `192.168.1.201`
   - المنفذ: `4370`
   - الفرع: اختر الفرع
4. انقر **اختبار الاتصال** للتحقق
5. انقر **مزامنة الآن** لسحب البصمات

---

## سابعاً: إضافة الموظفين

1. افتح **الموظفين** من القائمة
2. انقر **إضافة موظف**
3. أدخل:
   - الاسم الكامل
   - رقم الجهاز (ZK ID) — الرقم المسجل في جهاز البصمة
   - الراتب الأساسي
   - الشيفت والقسم والفرع

> **مهم:** رقم الجهاز (ZK ID) يجب أن يطابق الرقم المسجل في الجهاز

---

## ثامناً: إعداد قواعد الحضور

افتح **قواعد الحضور** وعدّل حسب سياسة الشركة:

| الإعداد | القيمة الافتراضية | الوصف |
|---------|-----------------|-------|
| وقت بداية الدوام | 09:00 | الوقت الرسمي للحضور |
| وقت نهاية الدوام | 17:00 | الوقت الرسمي للانصراف |
| فترة السماح | 20 دقيقة | الدقائق المسموح بها بعد بداية الدوام |
| وحدة الإضافي | 50 دقيقة | كل 50 دقيقة = ساعة إضافي |
| معامل الإضافي | 1.5 | OT = أجر الساعة × 1.5 |
| أيام الإجازة | 5,6 | 5=جمعة، 6=سبت |

---

## قاعدة الإضافي

```
كل 50 دقيقة = ساعة إضافي واحدة

20 دقيقة → 0 ساعة إضافي
49 دقيقة → 0 ساعة إضافي
50 دقيقة → 1 ساعة إضافي
110 دقيقة → 2 ساعة إضافي
160 دقيقة → 3 ساعات إضافي

قيمة الساعة الإضافية = أجر الساعة × 1.5
```

---

## التشغيل التلقائي للخدمات

عند فتح البرنامج يتم تلقائياً:
- ✅ تشغيل الخادم الخلفي
- ✅ فتح لوحة التحكم
- ✅ مزامنة الأجهزة كل 5 دقائق
- ✅ معالجة الحضور كل 10 دقائق
- ✅ ملخص يومي في منتصف الليل

---

## هيكل المشروع

```
ZKTeco/
├── backend/                 الخادم الخلفي (Node.js + Express)
│   ├── src/
│   │   ├── engines/
│   │   │   ├── attendanceEngine.js  محرك معالجة الحضور
│   │   │   ├── payrollEngine.js     محرك احتساب الرواتب
│   │   │   └── rulesEngine.js       محرك قواعد الحضور
│   │   ├── services/
│   │   │   ├── zktecoService.js     اتصال أجهزة البصمة
│   │   │   └── syncScheduler.js     جدولة المزامنة التلقائية
│   │   └── routes/                  واجهات API
│   └── prisma/
│       └── schema.prisma            هيكل قاعدة البيانات
├── frontend/                الواجهة (React + Electron)
│   ├── electron.js          البرنامج الرئيسي Electron
│   ├── preload.cjs          Preload script
│   └── src/
│       ├── pages/           صفحات التطبيق (كلها عربية RTL)
│       ├── engines/         منطق الحضور
│       └── lib/
│           ├── api.js       التواصل مع الخادم
│           └── agGridLocale.js  ترجمة الجداول للعربية
├── start-dev.bat            تشغيل وضع التطوير
├── build-app.bat            بناء النسخة النهائية
└── SETUP-AR.md              هذا الملف
```

---

## استكشاف الأخطاء

### البرنامج لا يفتح
```
تأكد من تثبيت Node.js 18+
node --version
```

### خطأ في الاتصال بقاعدة البيانات
```
تأكد من تشغيل MySQL
تأكد من صحة DATABASE_URL في backend/.env
```

### جهاز البصمة لا يتصل
```
تأكد أن الجهاز والكمبيوتر على نفس الشبكة
ping 192.168.1.201
تأكد أن المنفذ 4370 غير محجوب
```

### الأجهزة تظهر "غير متصل"
```
1. انقر على تعديل الجهاز
2. انقر اختبار الاتصال
3. إذا نجح، انقر مزامنة الآن
```

---

## الدعم التقني

للإبلاغ عن مشكلة أو طلب مساعدة، راجع:
- ملفات الـ logs في: `backend/logs/`
- سجلات البصمة الخام في قسم: السجلات الخام
