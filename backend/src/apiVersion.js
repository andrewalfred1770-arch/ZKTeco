// Contract version for the Hybrid Client/Server connection layer (EP-003).
// Bump only when an API-breaking change is made — independent of the product
// release version (package.json), which changes on every build.
module.exports = { API_VERSION: 1 };
